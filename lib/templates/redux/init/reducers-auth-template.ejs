// LIBS
import immutable from 'immutable'

// REDUX
import initialState from '../../initial-states/auth'
import {
} from '../../action-types/auth'

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
